anyio==4.14.0
certifi==2026.6.17
charset-normalizer==3.4.7
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
numpy>=1.26.0
packaging==26.2
pip==26.1.2
pluggy==1.6.0
pygments==2.20.0
pytest==9.1.1
python-dotenv==1.2.2
requests==2.34.2
semgrep>=1.78.0
typing-extensions==4.15.0
urllib3==2.7.0

# ---------------------------------------------------------------------------
# ZoneScan runtime dependencies (used when --zone-detect is passed)
# Install via: pip install -r requirements-zonescan.txt
# These are intentionally kept separate to keep the base image lightweight.
# ---------------------------------------------------------------------------
# torch>=2.2.0              # CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# transformers>=4.40.0      # HuggingFace model loading
# faiss-cpu>=1.8.0          # ANN search for M2+ FAISS index
# peft>=0.11.0              # LoRA adapter loading (M2+)
